[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "afm"
version = "0.1.0"
description = "Acoustic Failure Modes MRO"
dependencies = [
    "numpy",
    "pydub",
    "matplotlib",
    "scipy"
]
